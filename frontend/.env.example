# API URL is now determined dynamically at runtime based on window.location.hostname
# This allows the frontend to work from any IP/hostname without requiring a rebuild
# See lib/api.ts for details. No environment variable is needed.
