# ==============================================
# PORTAINER DEPLOYMENT - BIP2 Configuration
# ==============================================
# Instructions:
# 1. Copy these variables to Portainer's Environment Variables section
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit this file with real values to Git
# ==============================================

# --- REQUIRED: Gemini API Key ---
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# --- REQUIRED: JWT Secret ---
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_STRING_AT_LEAST_32_CHARS

# --- OPTIONAL: Azure AD Configuration ---
# Only needed if you want to use Microsoft Entra ID authentication
# Leave empty if not using Azure AD
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# ==============================================
# NOTES FOR PORTAINER DEPLOYMENT:
# ==============================================
# 
# How to use in Portainer Web Editor:
# 1. Go to Stacks → Add Stack → Web Editor
# 2. Paste the content from PORTAINER_DOCKER_COMPOSE.yml
# 3. Scroll down to "Environment variables" section
# 4. Add each variable above (one per line or use the advanced editor)
# 5. Format: VARIABLE_NAME=value (no quotes needed in Portainer UI)
# 6. Click "Deploy the stack"
#
# Security Best Practices:
# - Use strong passwords (min 32 characters for JWT_SECRET)
# - Rotate JWT_SECRET periodically
# - Never share or commit real API keys
# - Use Portainer's secrets management for sensitive data if available
#
# ==============================================
